apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'java.Main'

repositories {
    mavenLocal()
    mavenCentral()
 maven {
            url = "$buildDir/../../mvn-repo/"
        }
}

dependencies {
	compile files("$buildDir/../../money-library/build/libs/money-library.jar")
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}
